{
  "name": "laz-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n be,fe -c bgBlue.bold,bgGreen.bold \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "db:seed": "npm --prefix backend run db:seed",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:push": "npm --prefix backend run db:push"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
